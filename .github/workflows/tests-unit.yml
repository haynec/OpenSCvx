name: 🧪 Unit Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  unit-tests:
    if: ${{ !github.event.pull_request.draft }}
    name: Unit Tests (Python ${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15  # Unit tests should be fast

    strategy:
      fail-fast: false  # Continue testing other versions even if one fails
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ['3.11', '3.12', '3.13', '3.14']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true

    - name: Install dependencies
      run: |
        # Install package with optional GUI and test packages
        uv pip install --system -e ".[gui,test]" pytest-xdist

    - name: Run unit tests
      run: |
        pytest -v -m "not integration" --durations=15 --tb=short -n auto

    - name: Upload test results on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: unit-test-results-py${{ matrix.python-version }}-${{ matrix.os }}
        path: |
          .pytest_cache/
          test-results/
        retention-days: 7
